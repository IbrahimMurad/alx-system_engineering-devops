#!/usr/bin/env bash
# This script reloads gunicorn in graceful way

# Get the pid of the gunicorn process

pid=$(sudo systemctl status gunicorn | grep 'Main PID' | awk '{print $3}')

sudo kill -HUP "$pid"
